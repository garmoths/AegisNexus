# PDF Generation
reportlab>=4.0.0

# HTTP Client (HIBP Pwned Passwords, async-capable)
httpx>=0.27.0

# Web Framework
fastapi==0.128.0
uvicorn[standard]==0.40.0
# pydantic: tek pin, çelişkili constraint kaldırıldı
pydantic[email]==2.12.5
python-multipart>=0.0.9
email-validator>=2.0.0

# Database
sqlalchemy==2.0.45
psycopg2-binary==2.9.11
alembic>=1.13.0
aiofiles>=23.2.1

# Cache / Message Broker
vine>=5.1.0

# HTTP & Web
requests==2.32.5
certifi==2026.1.4
websocket-client==1.9.0

# Configuration
python-dotenv==1.2.1
celery[rabbitmq]==5.5.3

# LLM & AI
openai==1.55.0
groq==1.0.0
google-genai==1.74.0

# Web Scraping
beautifulsoup4==4.14.3
lxml==6.0.2
rapidfuzz==3.14.3

# Browser Automation / Vision Input
playwright==1.52.0

# ML — yerel phishing modeli (sklearn + numpy hafif; torch/easyocr kaldırıldı)
numpy>=1.24.0
scikit-learn>=1.3.0

# NOT: torch, transformers, easyocr kaldırıldı (image ~5GB → ~1.5GB).
# OCR (visual_analyzer.py) ve emotion pipeline (advanced_phishing_detector.py)
# ImportError'a düşer ve no-op/devam davranışı sergiler — guard'lı kod.
